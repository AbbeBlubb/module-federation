// Proxy pattern with added cache
// To be class
// Be sure to have this as singelton in the public app

// Remote to: public/App & modules/footer

export const fetchWithCacheProxy = function() {
    console.log("Hi from proxy!")
}

import React from "react";

const Header = () => (
    <header className="box">
        <h1>Public app will consume remotes!</h1>
        <p>This is a remote header</p>
    </header>
);

export default Header;
